version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    working_directory: /go/src/github.com/armeria-dev/armeria
    steps:
      - checkout
      - run:
          name: run unit tests
          command: go test -race ./...
